# Manual experiments

One-off scripts for local CrewAI / LiteLLM checks. **Not** part of CI or the FastAPI app.

Run from repo root with your virtualenv activated, for example:

```bash
cd alaa-ai-service && python scripts/manual/test_litellm_env.py
```
